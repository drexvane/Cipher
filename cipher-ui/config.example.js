/**
 * Cipher UI Configuration
 * 
 * Copy this file to config.js and update the values as needed.
 * Then include it in index.html before app.js:
 * <script src="config.js"></script>
 * <script src="app.js"></script>
 */

window.CIPHER_CONFIG = {
    // Base URL for the API (leave empty for same-origin)
    API_BASE_URL: '',
    
    // Alternative: specify a different backend
    // API_BASE_URL: 'https://api.example.com',
    
    // Optional: API key or authentication token
    // API_KEY: 'your-api-key-here',
    
    // Optional: Custom headers for all API requests
    // CUSTOM_HEADERS: {
    //     'X-Custom-Header': 'value'
    // },
    
    // Optional: Enable debug logging
    DEBUG: false,
    
    // Optional: Theme preference ('light', 'dark', 'system')
    DEFAULT_THEME: 'system'
};
